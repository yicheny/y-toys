import React from 'react';
import {Box, Button} from "../../../components";

export default function ButtonDemo() {
    return (
        <Box>
            <Button onClick={()=>null}>按钮</Button>
        </Box>
    );
}

